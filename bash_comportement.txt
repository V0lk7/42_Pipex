<"" "" | "" > ""

bash: : No such file or directory
bash: : No such file or directory

#####################################################################
< infile "" | "" > ""

bash: : No such file or directory

Command '' not found, but can be installed with:

apt install mailutils-mh  # version 1:3.7-2.1, or
apt install meshio-tools  # version 4.0.4-1
apt install mmh           # version 0.4-2
apt install nmh           # version 1.7.1-6
apt install termtris      # version 1.3-1

Ask your administrator to install one of them.

#####################################################################
< "" "" | "" > outfile

bash: : No such file or directory

Command '' not found, but can be installed with:

apt install mailutils-mh  # version 1:3.7-2.1, or
apt install meshio-tools  # version 4.0.4-1
apt install mmh           # version 0.4-2
apt install nmh           # version 1.7.1-6
apt install termtris      # version 1.3-1

Ask your administrator to install one of them.

#####################################################################
<infile cat | "" > ""
bash: : No such file or directory

outfile vide
#####################################################################

< infile "" | cat > ""

bash: : No such file or directory

Command '' not found, but can be installed with:

apt install mailutils-mh  # version 1:3.7-2.1, or
apt install meshio-tools  # version 4.0.4-1
apt install mmh           # version 0.4-2
apt install nmh           # version 1.7.1-6
apt install termtris      # version 1.3-1

Ask your administrator to install one of them.

#####################################################################
<infile "" | "" > outfile

Command '' not found, but can be installed with:

apt install mailutils-mh  # version 1:3.7-2.1, or
apt install meshio-tools  # version 4.0.4-1
apt install mmh           # version 0.4-2
apt install nmh           # version 1.7.1-6
apt install termtris      # version 1.3-1

Ask your administrator to install one of them.


Command '' not found, but can be installed with:

apt install mailutils-mh  # version 1:3.7-2.1, or
apt install meshio-tools  # version 4.0.4-1
apt install mmh           # version 0.4-2
apt install nmh           # version 1.7.1-6
apt install termtris      # version 1.3-1

Ask your administrator to install one of them.

#####################################################################
<infile cat | "" > outfile
Command '' not found, but can be installed with:

apt install mailutils-mh  # version 1:3.7-2.1, or
apt install meshio-tools  # version 4.0.4-1
apt install mmh           # version 0.4-2
apt install nmh           # version 1.7.1-6
apt install termtris      # version 1.3-1

Ask your administrator to install one of them

#####################################################################
<infile "" | cat > outfile
Command '' not found, but can be installed with:

apt install mailutils-mh  # version 1:3.7-2.1, or
apt install meshio-tools  # version 4.0.4-1
apt install mmh           # version 0.4-2
apt install nmh           # version 1.7.1-6
apt install termtris      # version 1.3-1

Ask your administrator to install one of them.

#####################################################################
<infile ls | "" > outfile
Command '' not found, but can be installed with:

apt install mailutils-mh  # version 1:3.7-2.1, or
apt install meshio-tools  # version 4.0.4-1
apt install mmh           # version 0.4-2
apt install nmh           # version 1.7.1-6
apt install termtris      # version 1.3-1

Ask your administrator to install one of them.

#####################################################################
<infile "" | ls > outfile
Command '' not found, but can be installed with:

apt install mailutils-mh  # version 1:3.7-2.1, or
apt install meshio-tools  # version 4.0.4-1
apt install mmh           # version 0.4-2
apt install nmh           # version 1.7.1-6
apt install termtris      # version 1.3-1

Ask your administrator to install one of them.

outfile full of ls redirection

#####################################################################

<infile "" | pdsdffe > outfile
Command '' not found, but can be installed with:

apt install mailutils-mh  # version 1:3.7-2.1, or
apt install meshio-tools  # version 4.0.4-1
apt install mmh           # version 0.4-2
apt install nmh           # version 1.7.1-6
apt install termtris      # version 1.3-1

Ask your administrator to install one of them.

pdwsft: command not found
outfile empty-->TRUNC

#####################################################################

<infile fuckit | "" > outfile
Command '' not found, but can be installed with:

apt install mailutils-mh  # version 1:3.7-2.1, or
apt install meshio-tools  # version 4.0.4-1
apt install mmh           # version 0.4-2
apt install nmh           # version 1.7.1-6
apt install termtris      # version 1.3-1

Ask your administrator to install one of them.

fuckit: command not found
outfile empty-->TRUNC

#####################################################################
<infile fuckit | ls > outfile
fuckit: command not found

outfile empty with ls redirection

#####################################################################
<infile fuckit | fuckit2 > outfile

fuckit: command not found
fuckit2: command not found

#####################################################################
:wq


